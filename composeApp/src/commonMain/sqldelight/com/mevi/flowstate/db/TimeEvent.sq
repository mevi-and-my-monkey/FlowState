CREATE TABLE TimeEvent (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    date INTEGER NOT NULL,
    isCountdown INTEGER NOT NULL DEFAULT 1
);

selectAll: SELECT * FROM TimeEvent;

insert: INSERT INTO TimeEvent (name, date, isCountdown) VALUES (?, ?, ?);

delete: DELETE FROM TimeEvent WHERE id = ?;