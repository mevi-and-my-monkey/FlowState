CREATE TABLE TimeEvent (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    targetDate TEXT NOT NULL,
    type TEXT NOT NULL CHECK(type IN ('SINCE', 'UNTIL'))
);

selectAll: SELECT * FROM TimeEvent;

insert: INSERT INTO TimeEvent (title, targetDate, type) VALUES (?, ?, ?);

delete: DELETE FROM TimeEvent WHERE id = ?;